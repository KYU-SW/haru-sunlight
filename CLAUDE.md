# 하루햇빛 — 작업 지침서 (디자인 유지)

> 이 저장소에서 작업하는 **사람과 AI(Claude Code 등) 모두**가 따르는 규칙입니다.
> AI는 작업을 시작할 때 이 파일을 먼저 읽고 아래 규칙을 지킵니다.

## 0. 가장 중요한 규칙

**디자인은 지금 모양을 그대로 유지한다.**

- 새 화면·기능은 **기존 화면(홈, 마이페이지)과 똑같은 모양**으로 만든다. 나란히 놓았을 때 다른 앱처럼 보이면 안 된다.
- **디자인을 바꾸라는 요청이 아니면 스타일을 바꾸지 않는다.** "예쁘게 해 줘", "개선해 줘"라는 요청도 아래 규칙 **안에서만** 한다.
- 규칙 밖의 변경(새 색, 새 글꼴, 그림자, 새 라이브러리 등)이 필요해 보이면 **하지 말고 먼저 사용자에게 묻는다.**
- **요청하지 않은 기능이나 화면을 임의로 추가하거나 되살리지 않는다.**

> 디자인이 자꾸 망가진 이유: 새 화면을 만들 때 옛 스타일(나눔고딕, 파란 그라데이션 헤더, 그림자 카드)이나 새 스타일(외부 UI 라이브러리)을 섞었기 때문입니다.

## 1. 색 — 아래 값만 쓴다 (새 색 추가 금지)

| 용도 | 값 |
|---|---|
| 제목·숫자 | `#131A2B` (`--ink`) |
| 라벨·보조 글자 | `#5B6478` (`--ink-2`) |
| 흐린 글자 | `#8E96A8` (`--ink-3`) |
| 구분선 | `rgba(19,26,43,.09)` (`--hair`) |
| 바탕 | `#FFFFFF` |
| 카드 면 | `#F3F6FB` (`--surf`) |
| **강조 파랑** | `#326CF9` — 버튼, 강조 단어, 켜진 상태 |
| 연한 파랑 | `#E3ECFF` / `#EAF0FF` — 강조 줄·보조 버튼 바탕 |

- 순수 검정(`#000`), 회색 계열 임의 값, 주황·초록 등 **다른 강조색을 만들지 않는다.**
- **그라데이션 금지.**

## 2. 글꼴

- **Pretendard만** 쓴다. 나눔고딕 등 다른 글꼴 금지.
- 굵기: 로고 900 / 큰 숫자·제목 800 / 라벨 700 / 본문 500~600
- 제목은 자간을 조인다(`letter-spacing:-0.04em` 정도, 로고는 `-0.07em`).
- 이모지는 Tossface(`.tf` 클래스). **최소한으로**, 홈 처방 카드의 그림 정도만.

> `css/style.css`는 옛 스타일 위에 새 스타일을 덧씌운 구조라 같은 선택자가 여러 번 나온다. 기준은 **이 문서와 CSS 파일의 맨 아래 블록**이다.

## 3. 모양

- **카드:** 그림자·테두리 **없이** 옅은 면(`#F3F6FB`), 모서리 **22px**. 클래스 `.ha-t`
- **버튼:** 파란 큰 버튼 높이 54px, 모서리 14px, 그림자 없음. 클래스 `.ha-cta`, `.btn`, `.btn-primary`
- **입력칸·검색칸:** 모서리 14px (`.st-search`)
- **간격:** 화면 좌우 16px, 카드 사이 10px (`.ha-wrap`)
- **구분:** 그림자 대신 **옅은 면 또는 가는 선(`--hair`)** 으로 나눈다.

## 4. 구조

- **하단 탭은 홈 · 타이머 · 마이페이지 3개 고정.** 탭을 늘리지 않는다.
- 분석, 노출 이력, 스탬프 같은 화면은 **마이페이지 안에서 들어가는 하위 화면**으로 만든다. 헤더는 `.an-head`(뒤로 가기 + 제목).
- **헤더:** 흰 바탕, 위에 고정. 구분선은 **평소엔 없고 스크롤할 때만** 생긴다(`#app.is-scrolled`).
- 홈에는 지역을 표시하지 않는다(지역은 마이페이지에서만).
- **화면 움직임:** 탭 전환은 페이드(0.22초), 하위 화면은 오른쪽에서 밀려 들어옴(0.32초), 온보딩은 단계가 바뀔 때만. **동작 줄이기 설정(`prefers-reduced-motion`)을 지킨다.**

## 5. 이미 있는 것을 쓴다 (새로 만들지 않는다)

| 필요한 것 | 쓸 것 |
|---|---|
| 알림 메시지 | `UI.toast('문구')` |
| 바닥에서 올라오는 창 | `UI.sheet(제목, 설명, html)` |
| 카드와 카드 라벨 | `.ha-t` / `.ha-lab` |
| 설정형 목록 | `.st-g`(묶음 제목) `.st-list` `.st-row` `.st-l` `.st-v` |
| 켜기·끄기 | `.sw` |
| 선택 버튼 묶음 | `.seg` |
| 하위 화면 머리·목록 | `.an-head` `.an-back` `.an-t`, `.an-card` `.an-split` `.an-row` |
| 검색·현재 위치 | `.st-search` `.st-geo` `.st-res` |
| 5단계 막대 | `.ha-bar` |

- 위 클래스를 **재사용**하고, 정말 없을 때만 `css/style.css` **맨 아래에** 새 규칙을 추가한다. 기존 규칙을 고칠 때는 다른 화면에 영향이 없는지 확인한다.
- 스타일은 `css/style.css`에만 쓴다. HTML 안에 `style="…"`를 늘리지 않는다.
- **React, Sonner, Tailwind 등 외부 UI 라이브러리·프레임워크를 넣지 않는다.** 이 앱은 번들러 없는 순수 HTML·CSS·JS이고, 토스트·시트가 이미 있다.

## 6. 문구

- **해요체**로 짧게. 핵심 단어만 파랑 + 굵게.
- **쓰지 않는 말:** "창" → **"햇빛 시간"**, "충분해요" 같은 과장, "비타민D가 합성됐어요" 같은 단정. (합성 여부는 측정하지 않는다. "권장 노출량 달성"으로 쓴다.)
- 회색 부설명을 남발하지 않는다. 화면이 잡다해지면 뺀다.
- **계산식과 계산 근거는 화면에 드러내지 않는다.** (근거는 `CALCULATION.md`)
- 데이터를 서버에 보내기 시작하면 "기록은 이 기기에만 저장돼요" 같은 문구가 **사실이 아니게 된다.** 함께 고친다.

## 7. 되살리지 않는 것 (이미 뺀 기능)

선크림(SPF) 선택, 생체리듬 · 기상 시간, 보충제 복용, 계산 근거 · 계산식 화면, 음식 대체 카드, 홈의 위치 표시, 하단의 "기록" 탭, 파란 그라데이션 헤더.

## 8. 작업 규칙

- **작업은 `co_work` 브랜치에서만 한다. `main`에 직접 올리지 않는다.** `main` 머지는 머지 담당만 한다.
- 커밋 제목은 `[v_0.x] …` 형식.
- 계산(`core/`)을 고쳤다면 `tests/smoke.html`이 **전부 PASS**여야 한다.
- 올리기 전에 `tests/seed.html?tab=home`으로 **홈 · 타이머 · 마이페이지**를 직접 확인하고, 새 화면을 **홈과 나란히 놓고 색·글꼴·모양을 비교**한다.
- **API 키, 비밀번호, 서비스 계정 키를 코드·커밋·채팅에 붙이지 않는다.** (기상청 키는 `config.local.js`에만, 배포 때는 저장소 비밀값으로 주입된다.)
- 앱은 **기기 저장이 먼저(오프라인 우선)** 다. 서버 연동이 있어도 타이머와 기록은 서버 없이 동작해야 한다.
- 같은 파일을 여러 명이 고치면 충돌이 난다. 푸시 전에 `git pull origin co_work`. 특히 `index.html`, `css/style.css`, `shared/app.js`는 조심한다.

## 9. 더 알아야 할 것

- 계산식과 출처: `CALCULATION.md`
- 프로젝트 소개와 실행: `README.md`
- 로컬 실행: `python -m http.server 8762` → `http://localhost:8762`
