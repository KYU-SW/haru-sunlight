/* =========================================================
   공개 설정 — 깃허브에 올라가도 되는 값만 둔다

   KMA_PROXY_URL : 기상청 API 중계 서버(worker/kma-proxy.js) 주소.
                   서비스키는 그 서버의 비밀값에만 있고 여기엔 없다.
                   링크로 들어온 사람은 이 주소를 통해 날씨를 받는다.

   로컬에서 config.local.js에 키를 넣어 두면 그 키로 기상청을 직접 부른다
   (중계 서버보다 우선).
   ========================================================= */
window.KMA_PROXY_URL = 'https://haru-sunlight-kma.haru-sunlight.workers.dev';
